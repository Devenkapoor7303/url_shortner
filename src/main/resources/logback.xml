<?xml version="1.0" encoding="UTF-8"?>
<configuration>

    <!-- Define the base directory and file name for logs -->
    <property name="LOG_DIR" value="logs" />
    <property name="LOG_FILE" value="application" />

    <!-- 1. FIXED CONSOLE APPENDER (Now allows ALL log levels) -->
    <appender name="CONSOLE_ALL" class="ch.qos.logback.core.ConsoleAppender">
        <!-- REMOVED THE LevelFilter SO ALL LOGS PRINT TO CONSOLE -->
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg%n</pattern>
        </encoder>
    </appender>

    <!-- 2. ROLLING FILE APPENDER (For all log entries) -->
    <appender name="ROLLING_FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>${LOG_DIR}/${LOG_FILE}.log</file>

        <rollingPolicy class="ch.qos.logback.core.rolling.SizeAndTimeBasedRollingPolicy">
            <fileNamePattern>${LOG_DIR}/archived/${LOG_FILE}-%d{yyyy-MM-dd}.%i.log.gz</fileNamePattern>
            <maxFileSize>10MB</maxFileSize>
            <maxHistory>30</maxHistory>
            <totalSizeCap>3GB</totalSizeCap>
        </rollingPolicy>

        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg%n</pattern>
        </encoder>
    </appender>

    <!-- 3. ASYNC APPENDER -->
    <appender name="ASYNC_FILE" class="ch.qos.logback.classic.AsyncAppender">
        <appender-ref ref="ROLLING_FILE" />
        <queueSize>512</queueSize>
        <discardingThreshold>0</discardingThreshold>
        <neverBlock>false</neverBlock>
        <includeCallerData>true</includeCallerData>
    </appender>

    <!-- 4. ROOT LOGGER BINDINGS -->
    <root level="INFO">
        <!-- Sends TRACE and above to your log file via async thread -->
<!--        <appender-ref ref="ASYNC_FILE" />-->
        <!-- Sends TRACE and above to your console screen immediately -->
        <appender-ref ref="CONSOLE_ALL" />
    </root>

</configuration>